{
  "routes": [
    {
      "method": "GET",
      "path": "/photos",
      "handler": "Photo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photos/count",
      "handler": "Photo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photos/:_id",
      "handler": "Photo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/photos",
      "handler": "Photo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/photos/:_id",
      "handler": "Photo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/photos/:_id",
      "handler": "Photo.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}